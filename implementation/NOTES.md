# NOTES.md

> Hardware emulation reference: Before implementing any Atari 800 XL PAL machine related hardware emulation, use the [AHRM](/AHRM/index.md) as reference.

Simple implementation notes for this repository.

Reference: follow `AGENTS.md`.
Process rule: review this file before planning any improvement, and update it after each code improvement.

## Project Paths
- `A8E/` -> native C implementation.
- `jsA8E/` -> browser JavaScript implementation.
- `implementation/A8E/` -> native C implementation notes.
- `implementation/jsA8E/` -> browser JavaScript implementation notes.

## Update Rules
- Keep entries short and practical.
- Add/update notes when behavior or structure changes.
- Maintain a short `Files` list for fast navigation.

## Notes Template
- Files: key files to open first.
- Purpose: what this area is responsible for.
- Status: verified implementation state (`implemented`, `partial`, or `pending`).
- Notes: short implementation details (simplified).
- Issues: known issues, limitations, or missing implementation items.
- Todo: next improvements or checks.

## A8E (C) Implementation Notes
- [CPU (6502)](A8E/CPU.md)
- [ANTIC](A8E/ANTIC.md)
- [GTIA](A8E/GTIA.md)
- [POKEY](A8E/POKEY.md)
- [PIA](A8E/PIA.md)
- [Atari I/O and System Glue](A8E/SYSTEM.md)
- [Debug](A8E/DEBUG.md)

## jsA8E (JavaScript) Implementation Notes
- [Core Emulation](jsA8E/CORE.md)
- [Input and Host/Device Integration](jsA8E/INPUT_HOST.md)
- [Rendering / CRT](jsA8E/RENDER.md)
- [Audio](jsA8E/AUDIO.md)
- [Automation / Public Machine API](jsA8E/AUTOMATION.md)
- [UI / Interface](jsA8E/UI.md)
- [Worker Boundary](jsA8E/WORKER.md)
- [Debug](jsA8E/DEBUG.md)

## Recent Improvements
- 2026-03-14: `jsA8E/js/core/atari.js`, `jsA8E/js/app/ui.js`, `jsA8E/emulator_worker.js`, `implementation/jsA8E/RENDER.md`, `implementation/NOTES.md`: reduced visible jsA8E screen tearing during frame delivery. The main frame loop now coalesces catch-up rendering to a single `paint()` per browser RAF tick (show latest completed frame instead of presenting multiple frames in one callback), and WebGL context creation in both main-thread and worker paths no longer requests `desynchronized` low-latency mode.
- 2026-03-14: `A8E/AtariIo.c`, `jsA8E/js/core/antic.js`, `jsA8E/tests/antic_display_list_nmi.test.js`, `jsA8E/package.json`, `implementation/{A8E/ANTIC.md,jsA8E/CORE.md,NOTES.md}`: aligned ANTIC display-list/NMI edge behavior with AHRM in both cores. `JVB` decoding now ignores the DLI/VS bits (`$41/$C1/...`) instead of matching only `$41`, `JVB+DLI` replay now re-arms one DLI per scanline until VBL, and `NMIST` DLI status now persists until scanline 248 (or `NMIRES`) instead of being cleared every scanline. Added a focused jsA8E regression test covering JVB+DLI wait-for-VBL semantics and NMIST DLI lifetime.
- 2026-03-14: `legacy/COLOR_CLOCK_ACCURACY.md`, `implementation/NOTES.md`: revised the color-clock accuracy tracker to use the AHRM as the ground of truth for hardware emulation accuracy instead of the legacy renderer. The document now cites specific AHRM sections for scanline timing, DMA steal positions, NMI/WSYNC deadlines, and P/M graphics behavior, adds an AHRM timing-chart audit step to the remaining work, and demotes the legacy renderer to a secondary reference.
- 2026-03-14: `A8E/AtariIo.c`, `jsA8E/js/core/playfield/playfield.js`, `implementation/NOTES.md`: fixed non-wide horizontally scrolled playfield display width in both cores. Rendering still uses the wider HSCROL fetch/timing geometry, but now clips the fetched playfield output back to the normal visible narrow/normal aperture so mode lines do not appear wider when `HSCROL` is enabled (including ANTIC mode 4 content such as `Schraenker3.atr`), while keeping existing DMA/timing behavior intact.
- 2026-03-14: `AGENTS.md`, `README.md`, `CHANGELOG.md`, `A8E/README.md`, `jsA8E/{README,AUTOMATION}.md`, `legacy/COLOR_CLOCK_ACCURACY.md`, `implementation/{NOTES,A8E/*.md,jsA8E/*.md}`: aligned non-AHRM documentation with the current emulator hardware-emulation policy by adding a consistent AHRM-first reference note for Atari 800 XL PAL machine related hardware emulation work.
- 2026-03-14: `AHRM/index.md`, `implementation/NOTES.md`: encoded AHRM markdown link destinations for GitHub rendering (`%20`, `%28`, `%29`, `%27`) so links with spaces/parentheses/apostrophes are clickable in the GitHub web UI while still resolving to the same local files.
- 2026-03-14: `AHRM/index.md`, `README.md`, `implementation/NOTES.md`: renamed the AHRM index file to lowercase `index.md` for GitHub-friendly path conventions, updated root README to point to the new path, and kept all AHRM section links relative so they resolve correctly from within the AHRM folder.
- 2026-03-14: `AHRM/index.md`, `implementation/NOTES.md`: fixed AHRM index link targets to be relative to `AHRM/index.md` by removing the extra leading `AHRM/` path prefix, so section links now resolve to the existing sub-markdown files.
- 2026-03-14: `README.md`, `implementation/NOTES.md`: added a new root-level Hardware Reference section that points to the local `AHRM/index.md`, explicitly links the Altirra Hardware Reference Manual PDF, and thanks its author Avery Lee.
- 2026-03-14: `jsA8E/js/app/automation_api.js`, `jsA8E/js/core/{memory,atari,app_proxy}.js`, `jsA8E/emulator_worker.js`, `jsA8E/tests/automation_memory_helpers.test.js`, `jsA8E/AUTOMATION.md`, `implementation/jsA8E/AUTOMATION.md`, `implementation/NOTES.md`: added debug memory write/wait automation helpers and wired them through both main-thread and worker backends. New automation methods are `writeMemory(address, value)`, `writeRange(start, data)`, `writeWord(address, value, options)`, and `waitForMemory(options)` (masked byte/word polling with timeout artifacts). Added capability flags `memoryWrite` and `memoryWait`, plus regression coverage for byte/range/word writes and memory wait success paths.
- 2026-03-14: `jsA8E/AUTOMATION.md`, `implementation/jsA8E/AUTOMATION.md`, `implementation/NOTES.md`: synchronized automation documentation with the live `automation_api.js` contract. Clarified root `events` availability, expanded `getCapabilities()` flag coverage (`screenshot`, `artifacts`, `cacheControl`, `waitPrimitives`, etc.), documented the `mountDisk(data, nameOrOpts, slot)` compatibility signature, called out `readRange` wrap behavior, and added an explicit note that grouped methods still have root-level flat aliases (including `releaseAllKeys`).
- 2026-03-14: `jsA8E/js/app/automation_api.js`, `jsA8E/tests/automation_memory_helpers.test.js`, `jsA8E/package.json`, `jsA8E/AUTOMATION.md`, `implementation/jsA8E/AUTOMATION.md`, `implementation/NOTES.md`: added 16-bit memory convenience helpers to the public automation debug API. `readWord(address, options)` now reads unsigned 16-bit values with optional big-endian decoding (`{ littleEndian: false }`), `readWordSigned(address, options)` returns signed 16-bit values, and both methods wrap across `$FFFF -> $0000` consistently through the existing range-read path. Added a focused automation test and wired it into `npm run test:automation`.
- 2026-03-13: `A8E/{6502.h,6502.c}`, `implementation/NOTES.md`: aligned native C CPU NMI handling with the jsA8E fix for the `Schraenker3.atr` "2023" stall path. `_6502_Nmi()` now sets a pending NMI edge, `_6502_Execute()` services that edge once at the instruction boundary, nested NMI re-entry is blocked with an active-NMI flag until `RTI`, and reset clears the new NMI pending/active flags.
- 2026-03-13: `jsA8E/js/core/{cpu,atari,memory}.js`, `implementation/NOTES.md`: fixed a jsA8E NMI re-entry issue that stalled `Schraenker3.atr` on the "2023" screen. `CPU.nmi()` now raises a single pending edge instead of forcing immediate nested interrupt entry, servicing now happens once at instruction boundaries, and additional NMI edges are deferred until the active NMI returns via `RTI`. Added `nmiPending`/`nmiActive` reset handling and snapshot save/load support so paused/restored machines keep consistent interrupt state.
- 2026-03-13: `jsA8E/js/core/antic.js`, `implementation/NOTES.md`: fixed jsA8E ANTIC NMI handling to match the C core. Restored per-scanline `NMI_DLI` clear and DLI/VBI mutual exclusion while removing the incorrect NMIST-based edge-trigger guard that prevented boot. The guard checked the NMIST status register instead of the NMI pin level; since NMIST bits accumulate before the OS enables NMIEN, the guard blocked the first VBI after enable and the system never booted. NMI is now fired unconditionally when NMIEN allows, matching `AtariIo.c`.
- 2026-03-12: `A8E/{6502.h,6502.c,AtariIo.c}`, `jsA8E/js/core/{cpu,state,playfield_renderer,atari}.js`, `implementation/A8E/ANTIC.md`, `implementation/jsA8E/CORE.md`, `implementation/NOTES.md`: fixed the follow-up vertical timing regression from the earlier DLI seam fix. The root issue was using one shared `ioCycleTimedEventCycle` threshold in both the CPU/master loop and the beam/scanline loop even though DLI deadlines now live on the beam clock while timer/SIO deadlines stay on the master clock. Both cores now track separate beam/master next-event thresholds, keep the old combined threshold only for waking the CPU while not drawing, and recompute scheduler state after JS snapshot restore.
- 2026-03-12: `A8E/AtariIo.c`, `jsA8E/js/core/antic.js`, `implementation/A8E/ANTIC.md`, `implementation/jsA8E/CORE.md`, `implementation/NOTES.md`: compared a DLI-driven mode-4 picture loader (`image.xex`) against `legacy/A8E_experimental` and fixed horizontal charset-band seams in both cores. The regression came from scheduling and dispatching DLI on the CPU/master `cycleCounter`; with per-scanline DMA steals that moved `WSYNC`-driven `CHBASE` changes inside the wrong raster slot. DLI deadlines now use the beam clock again (`llCycle` in native C, `io.clock` in jsA8E), matching the legacy raster path while leaving the other timer/SIO deadlines on the master cycle domain.
- 2026-03-12: `jsA8E/js/core/atari.js`, `jsA8E/emulator_worker.js`, `jsA8E/js/core/app_proxy.js`, `jsA8E/js/app/ui.js`, `jsA8E/js/app/snapshot_ui.js`, `implementation/jsA8E/{UI,WORKER}.md`: fixed snapshot GUI restore and save UX. The core app now exposes config getters (`getTurbo`, `getSioTurbo`, `getAudioEnabled`, `getOptionOnStart`, `getKeyboardMappingMode`), the worker state snapshot carries config fields, and the proxy tracks them so `updateStatus()` can reconcile toggle buttons (turbo, SIO turbo, audio, option on start, keyboard mapping) with the restored config state after a snapshot load. Save and export now resume the emulator after capturing when it was running before, instead of leaving it paused.
- 2026-03-12: `README.md`, `jsA8E/README.md`, `jsA8E/AUTOMATION.md`, `implementation/jsA8E/AUTOMATION.md`, `implementation/NOTES.md`: refreshed automation documentation wording after the new headless bootstrap landed. The docs now explicitly position `jsA8E/headless.js` as the preferred integration path for agent/CI/browser-less automation while keeping the browser `window.A8EAutomation` facade documented as the equivalent in-page entrypoint.
- 2026-03-12: `jsA8E/headless.js`, `jsA8E/tests/headless_automation.test.js`, `jsA8E/package.json`, `jsA8E/AUTOMATION.md`, `jsA8E/README.md`, `README.md`, `implementation/jsA8E/AUTOMATION.md`, `implementation/NOTES.md`: added a supported browser-less jsA8E bootstrap for Node automation. The new headless entrypoint loads the existing script graph into a `vm` context, creates the no-worker backend with a minimal 2D/screenshot host, attaches the normal grouped automation API, supports optional ROM path injection, and is now covered by an end-to-end regression test that boots the real core, exercises HostFS, assembly, start/pause, screenshots, and snapshot round-trips.
- 2026-03-12: `jsA8E/js/core/{atari,debugger}.js`, `jsA8E/js/app/automation_api.js`, `jsA8E/AUTOMATION.md`, `jsA8E/tests/{automation_snapshot_api,snapshot_save_timing}.test.js`, `jsA8E/package.json`, `implementation/jsA8E/{CORE,AUTOMATION}.md`: changed jsA8E snapshot saves to align paused machines to the next frame boundary before encoding, which avoids intermittent bad restores from mid-raster save points. Added a cycle-exact opt-out via `timing: "exact"`, passed that through automation, and covered both the core and automation paths with regression tests.
- 2026-03-12: `jsA8E/js/core/{hostfs,hdevice}.js`, `jsA8E/tests/hdevice_snapshot_state.test.js`, `jsA8E/package.json`, `implementation/jsA8E/{CORE,INPUT_HOST}.md`: fixed jsA8E snapshot completeness for H:. Snapshot save/load now includes persisted HostFS file contents and metadata through the existing H: snapshot payload instead of restoring only open IOCB channel state, and a regression test now covers the file-state round-trip.
- 2026-03-12: `jsA8E/js/app/ui.js`, `implementation/jsA8E/UI.md`: refined the jsA8E top-bar start/pause interaction so lifecycle clicks now enter a short busy state. The button flips immediately to the requested play/pause state with a busy label, temporary lifecycle-button locking prevents double-click races, and the toolbar reconciles back to the live running state once the request settles.
- 2026-03-12: `jsA8E/AUTOMATION.md`, `README.md`, `jsA8E/README.md`, `implementation/jsA8E/AUTOMATION.md`, `implementation/NOTES.md`: added a dedicated public jsA8E automation API reference and aligned the root README, jsA8E README, and implementation note around the same current grouped `window.A8EAutomation` surface, including the new public-document split, root version fields, and the live `attached` event.
- 2026-03-12: `README.md`, `jsA8E/README.md`, `implementation/jsA8E/AUTOMATION.md`: refreshed the automation documentation to match the live browser API. The docs now describe the grouped `window.A8EAutomation` surface as implemented today, including snapshot save/load, HostFS file-state helpers, current event subscriptions, capability/state reporting, and the structured failure/system-state behavior used by the current automation facade.
- 2026-03-12: `jsA8E/style.css`, `implementation/jsA8E/UI.md`: aligned the jsA8E brand block to the toolbar’s 44px primary-control height so the `jsA8E` label sits in the same vertical band as the first-row button text instead of reading too high against the top edge.
- 2026-03-12: `jsA8E/style.css`, `implementation/jsA8E/UI.md`: changed the jsA8E brand headline row from centered/icon-box alignment to baseline alignment so the `jsA8E` text follows the surrounding text baseline instead of being visually centered too high.
- 2026-03-12: `jsA8E/style.css`, `implementation/jsA8E/UI.md`: nudged the jsA8E headline text down by 1px so the `jsA8E` label aligns better with the surrounding icon/button text after the earlier headline alignment pass.
- 2026-03-12: `jsA8E/style.css`, `implementation/jsA8E/UI.md`: aligned the jsA8E headline `jsA8E` text with the adjacent help icon by tightening the title line-height and giving the title row a stable 18px control-height baseline.
- 2026-03-12: `jsA8E/index.html`, `implementation/jsA8E/UI.md`: refreshed the jsA8E `?` help tooltip icon legend so it matches the current toolbar states and newer layered icons. The legend now shows play/pause, audio on/off, expand/collapse chevrons, enter/exit fullscreen, turbo badge variants, hidden joystick/keyboard icons, and the current `Option on Start` label instead of the older one-state menu glyph list.
- 2026-03-12: `jsA8E/index.html`, `jsA8E/style.css`, `jsA8E/js/app/ui.js`, `implementation/jsA8E/UI.md`: reorganized the jsA8E top toolbar around four primary controls. `Load Disk`, run/pause, reset, and audio now stay in the main row, the remaining ROM/toggle/panel buttons moved into a secondary row below, and a plain chevron button at the upper right now collapses or expands that secondary toolbar without affecting control behavior.
- 2026-03-12: `jsA8E/js/app/ui.js`, `implementation/jsA8E/UI.md`: made the jsA8E run/pause button optimistic again instead of waiting for worker lifecycle acknowledgements before updating the toolbar state. The control now flips immediately based on the requested state and relies on the existing live state/debug updates to reconcile later, which avoids the pause button appearing to hang when the worker takes a moment to service the request.
- 2026-03-12: `jsA8E/style.css`, `implementation/jsA8E/UI.md`: increased the jsA8E snapshot panel's default vertical working space. `snapshotPanel` now opens with a real minimum height and stretchable body area instead of collapsing to its compact content height, which keeps the quick-save/export/import controls usable when the panel is opened in the normal layout flow.
- 2026-03-12: `jsA8E/js/app/ui.js`, `implementation/jsA8E/UI.md`: fixed the top-bar run/pause button to await async start/pause transitions before redrawing its state. This avoids the button/icon getting out of sync in worker-backed flows and also guards against rapid double-clicks during a pending lifecycle transition.
- 2026-03-12: `jsA8E/index.html`, `jsA8E/style.css`, `jsA8E/js/app/{ui,snapshot_ui}.js`, `implementation/jsA8E/UI.md`: added a dedicated jsA8E snapshot UI. The top toolbar now includes a snapshot icon that opens a compact panel with browser-backed quick save/load (single IndexedDB quick slot) plus snapshot export/import actions, all routed through the same core/automation snapshot APIs instead of a separate UI-only save path.
- 2026-03-12: `jsA8E/js/core/{snapshot_codec,atari,memory,debugger,input,hdevice}.js`, `jsA8E/js/core/app_proxy.js`, `jsA8E/emulator_worker.js`, `jsA8E/js/app/automation_api.js`, `jsA8E/tests/{automation_snapshot_api,snapshot_codec_roundtrip}.test.js`, `jsA8E/package.json`, `implementation/jsA8E/{CORE,AUTOMATION,WORKER}.md`: added jsA8E snapshot automation. The browser core can now save/load full paused machine snapshots through a versioned binary codec, including CPU/RAM/shadow RAM, video buffers, I/O/custom-chip state, mounted media/ROM contents, debugger state, input bookkeeping, and H: channel state. Worker RPC now transports `saveSnapshot` / `loadSnapshot`, and `window.A8EAutomation.system.saveSnapshot/loadSnapshot` expose the feature with pause-before-save/load semantics and resumable `savedRunning` metadata.
- 2026-03-11: `jsA8E/js/core/memory.js`, `jsA8E/tests/memory_xex_preflight_bank_switch.test.js`, `implementation/jsA8E/CORE.md`: aligned jsA8E XEX preflight with the live `Option-on-Start` boot policy. If no explicit `PORTB` override is provided, BASIC-area overlap checks now assume BASIC is already disabled when the UI/automation boot setting holds OPTION on startup, matching native A8E's BASIC-off default boot behavior more closely and removing false BASIC-ROM overlap failures for many XEX loads.
- 2026-03-11: `jsA8E/js/app/ui.js`, `implementation/jsA8E/UI.md`: adjusted `Load Disk` UI semantics after the earlier mount-flow unification. The file-picker path still uses the awaitable mount call when available, but no longer auto-resets or auto-starts the emulator so it remains usable for plain disk flipping. Drag-and-drop keeps the immediate reboot/start behavior.
- 2026-03-11: `jsA8E/js/core/atari.js`, `implementation/jsA8E/CORE.md`: fixed jsA8E reset-time `PORTB` default wiring. The browser core was incorrectly treating `IO_INIT_VALUES` like an address-indexed table when constructing `A8EMemory`, which collapsed the XEX preflight default banking state to sanitized `0x7C` and caused false self-test-ROM overlap failures for some XEX loads. It now looks up the real `IO_PORTB` init entry and passes the intended XL default (`$FD`).
- 2026-03-11: `jsA8E/js/app/ui.js`, `implementation/jsA8E/UI.md`: unified browser `Load Disk` behavior with drag-and-drop. The file-picker path now awaits the detailed mount call when available and runs the same reboot/start flow afterward, so loading a second ATR/XEX into `D1:` while another image is already mounted reliably boots the new media instead of only changing the slot contents under the current session.
- 2026-03-11: `jsA8E/js/core/memory.js`, `jsA8E/tests/memory_xex_preflight_bank_switch.test.js`, `jsA8E/package.json`, `implementation/jsA8E/CORE.md`: fixed jsA8E XEX mount preflight to follow Atari loader ordering instead of rejecting whole files on static ROM/I/O range overlap. The browser loader now simulates XEX writes in order, treats `D301` bank-switch writes as valid loader input, and only reports protected-memory failures when a later byte still targets ROM-backed space after the current `PORTB` state is considered. Added a regression test that covers `D301`-driven self-test/BASIC banking and keeps true blocked self-test writes failing.
- 2026-03-11: `jsA8E/js/core/app_proxy.js`, `jsA8E/emulator_worker.js`, `jsA8E/js/app/{automation_api,ui}.js`, `jsA8E/tests/{automation_system_state_resilience,automation_xex_boot_failure}.test.js`, `jsA8E/package.json`, `jsA8E/README.md`, `implementation/jsA8E/{AUTOMATION,WORKER}.md`: hardened jsA8E automation around the worker boundary. Worker `start/pause/reset` now use acknowledged RPC instead of fire-and-forget commands, pending worker requests time out with command-specific errors, `mountDisk` prefers the detailed request/response mount path, `dev.runXex(...)` emits an explicit mount checkpoint and awaits reset/start acknowledgements, and `getSystemState({ timeoutMs })` now returns partial state plus structured error details when one backend read stalls instead of hanging forever. The UI/app bootstrap also now supports an explicit no-worker fallback via `?a8e_worker=0` or `window.A8E_BOOT_OPTIONS.worker = false`.
- 2026-03-11: `jsA8E/js/core/{playfield,playfield_renderer}.js`, `jsA8E/index.html`, `jsA8E/emulator_worker.js`, `implementation/jsA8E/CORE.md`: continued the rendering split by extracting the ANTIC per-mode playfield pixel generators, blank-line/background helpers, and line-clock stepping into `playfield_renderer.js`. `playfield.js` now focuses on active-line geometry, scratch-buffer orchestration, and display-memory row ownership while browser and worker script bootstraps both load the new renderer module before `playfield.js`.
- 2026-03-11: `jsA8E/js/app/automation_api.js`, `jsA8E/js/app/automation/utils.js`, `jsA8E/index.html`, `jsA8E/tests/{automation_url_media_loading,automation_xex_boot_failure}.test.js`, `implementation/jsA8E/AUTOMATION.md`: started the planned `automation_api.js` split by extracting shared binary normalization, cache-control fetch setup, text/base64 conversion, and structured automation error helpers into `js/app/automation/utils.js`. The browser page and Node automation tests now load the helper module before the facade, and `automation_api.js` now focuses on API orchestration instead of low-level utility code.
- 2026-03-11: `jsA8E/js/app/automation_api.js`, `jsA8E/tests/automation_url_media_loading.test.js`, `jsA8E/package.json`, `jsA8E/README.md`, `implementation/jsA8E/AUTOMATION.md`: extended jsA8E automation asset loading with URL-native ROM helpers (`loadRomFromUrl`, `loadOsRomFromUrl`, `loadBasicRomFromUrl`) that reuse the same cache/version fetch controls as `mountDiskFromUrl` and `runXexFromUrl`. Capability discovery now exposes `urlRomLoad`, `urlDiskLoad`, and `urlXexLoad`, and a new Node regression test covers cache-busted ROM fetches plus the bytes handed into the OS/BASIC ROM loaders.
- 2026-03-11: `jsA8E/js/core/{memory,atari,app_proxy,debugger}.js`, `jsA8E/js/app/automation_api.js`, `jsA8E/emulator_worker.js`, `jsA8E/tests/automation_xex_boot_failure.test.js`, `jsA8E/package.json`, `implementation/jsA8E/{AUTOMATION,WORKER}.md`: reworked jsA8E XEX automation boot failure handling. XEX mounts now run through a structured preflight report that captures segment ranges, inferred RUN/INIT vectors, boot-buffer placement, and overlap with loader/I/O/ROM-backed regions under the requested reset `PORTB` state. Worker RPC now preserves structured loader errors instead of collapsing them to strings. `window.A8EAutomation.dev.runXex(...)` now reports `xex_preflight_passed` / `xex_preflight_failed`, returns structured `xex_boot_failed` artifacts with explicit boot diagnostics (`bootState`, `bootDiagnostics`, bank/media/trace/disassembly context), and when an entry PC is known it uses paused `runUntilPc(...)` with optional tight-loop detection instead of letting failed boots degrade into generic wait timeouts. Added a headless Node regression test that verifies a broken XEX launch reports a structured boot failure and does not emit success-style boot checkpoints after preflight failure.
- 2026-03-11: `jsA8E/js/core/playfield.js`: fixed jsA8E ANTIC Modes 4 and 5 alternate-color-set selection. The JS renderers were incorrectly routing character bytes through `decodeTextModeCharacter` (which applies CHACTL-dependent Mode 2/3 logic), so bit 7 of the character byte was not treated as the alternate palette selector when CHACTL was 0. Now both modes directly test bit 7 to choose between normal (COLPF2) and alternate (COLPF3) color tables, matching the native C implementation and real hardware behavior.
- 2026-03-11: `jsA8E/js/core/playfield.js`, `implementation/jsA8E/CORE.md`: fixed jsA8E ANTIC character-mode vertical-scroll row fetches so modes 2-7 blank out-of-range glyph rows instead of reading adjacent character data. Added shared row helpers for 8-line, 10-line (descender), and 16-line text modes, which removes the stray horizontal artifact lines visible on scrolled graphics screens.
- 2026-03-10: `jsA8E/js/core/{memory,atari,app_proxy}.js`, `jsA8E/emulator_worker.js`, `jsA8E/js/app/automation_api.js`, `jsA8E/README.md`, `implementation/jsA8E/{AUTOMATION,WORKER}.md`: added reset-time automation overrides for initial PIA banking state. `memory.hardReset(options)` now accepts an initial `portB` value that is applied before the cold-reset memory map is built, and that path is now wired through main-thread and worker `app.reset(...)`. `window.A8EAutomation.system.reset/boot` and `dev.runXex(...)` now accept reset options (including `resetOptions.portB` or a top-level `portB`) so automation can force self-test ROM hidden or visible before XEX boot starts, with feature discovery exposed via `getCapabilities().resetPortBOverride`.
- 2026-03-10: `A8E/AtariIo.c`, `A8E/Pokey.c`, `jsA8E/js/core/{antic,pokey,pokey_sio}.js`: backed recent `legacy/A8E_experimental`-inspired event timing away from the in-line beam clock after longer runs still drifted. DLI, POKEY timer deadlines, and SIO deadlines now all stay anchored to the master emulation `cycleCounter` in both cores, while the newer draw-path geometry/memory fixes remain in place. This keeps the renderer improvements without depending on beam-clock event ownership that the current instruction-granular CPU core cannot hold stable over time.
- 2026-03-10: `A8E/AtariIo.{c,h}`, `jsA8E/js/core/{antic,playfield,state}.js`: fixed first-scanline detection for VSCROL-adjusted display-list blocks. Replaced the computed `isFirstModeLine`/`bFirstModeLine` check (which compared `lineDelta === numberOfLines` and failed when VSCROL reduced the block height) with a `bFirstRowScanline`/`firstRowScanline` flag set once at display-list fetch time and cleared after the first scanline draw. This matches the legacy `lLine == 0` semantics and restores correct DMA steal timing and display-memory advancement for vertically scrolled content.
- 2026-03-10: `A8E/AtariIo.{c,h}`, `jsA8E/js/core/{antic,playfield,state}.js`: corrected the ANTIC row-memory model to match `legacy/A8E_experimental` without restoring the old whole-row renderer. Both cores now snapshot a persistent row-base display-memory address when a display-list mode line starts, render every scanline in that ANTIC block from that row base, and advance the global `displayMemoryAddress` once from the generic line wrapper after the first scanline has been drawn. This avoids the earlier “advance on last scanline” drift without corrupting the later scanlines of the same row.
- 2026-03-10: `A8E/AtariIo.c`, `A8E/Pokey.c`, `jsA8E/js/core/{antic,pokey,pokey_sio}.js`: aligned current C and JS raster-event timing with `legacy/A8E_experimental` for DLI-driven graphics effects while keeping the newer per-clock renderer. Both cores now schedule DLI deadlines from the beam clock at display-list fetch time, create POKEY/SIO deadlines from the same effective hardware clock (`beam clock` in-line, `cycleCounter` otherwise), and use legacy-style post-line scanline advancement: `currentDisplayLine` now advances after the line draw completes, `NMI_DLI` is cleared on each line step, VBI is raised after the first post-visible blank line, and frame wrap resets the display-list command / priority state instead of relying on the older whole-frame draw path.
- 2026-03-10: `jsA8E/js/app/automation_api.js`, `jsA8E/js/core/{input,atari,app_proxy}.js`, `jsA8E/emulator_worker.js`, `jsA8E/README.md`, `implementation/jsA8E/{AUTOMATION,WORKER}.md`: expanded `window.A8EAutomation` with URL-native media loaders (`mountDiskFromUrl`, `runXexFromUrl`), cross-realm-safe binary normalization for automation inputs/outputs, structured timeout/failure snapshots (`captureFailureState`, `runUntilPcOrSnapshot`) using artifact schema v2, progress events/cache-busting reload support, and explicit automation-visible console-key state/query helpers. Worker RPC now forwards `getConsoleKeyState`, and wait helpers return rich diagnostic bundles on timeout instead of only a generic error string.
- 2026-03-10: `README.md`, `A8E/README.md`, `jsA8E/README.md`, `legacy/COLOR_CLOCK_ACCURACY.md`, `implementation/jsA8E/CORE.md`: refreshed the raster-accuracy documentation to match the current code. The READMEs no longer claim border/playfield geometry and non-wide `HSCROL` timing are still missing, `legacy/COLOR_CLOCK_ACCURACY.md` now serves as a verification/signoff tracker instead of an outdated implementation-gap list, and the jsA8E core note now reflects that the remaining work is regression verification rather than missing native parity.
- 2026-03-10: moved the raster-accuracy verification tracker from the repo root to `legacy/COLOR_CLOCK_ACCURACY.md` and updated README references so the file now lives alongside the legacy renderer sources it references.
- 2026-03-09: `jsA8E/js/app/automation_api.js`, `jsA8E/js/core/app_proxy.js`, `jsA8E/emulator_worker.js`, `jsA8E/README.md`, `implementation/jsA8E/WORKER.md`: added an explicit worker `getDebugState` request path and updated the automation facade to prefer it, so `window.A8EAutomation.debug.getDebugState()` no longer depends only on the last pushed worker snapshot. The jsA8E automation example now assembles a self-looping `JMP START` sample, sets a breakpoint at `$2000`, and waits for that pause before querying source context and disassembly, so the documented flow lands on assembled user code instead of relying on a timing-sensitive real-time sleep.
- 2026-03-09: `jsA8E/js/app/automation_api.js`, `jsA8E/js/core/{cpu,debugger,atari,app_proxy}.js`, `jsA8E/emulator_worker.js`: completed the browser-first automation surface around `window.A8EAutomation`. The public API is now grouped into `system/media/input/debug/dev/artifacts/events` (with compatibility aliases), adds `apiVersion`/`getCapabilities()`/`getSystemState()`, wait helpers, HostFS + assembler automation (`assembleSource`, `assembleHostFile`, `runXex`), source-context/disassembly lookup, joystick/console-key control, worker parity for mounted-media queries, and explicit stop/fault reporting (`fault_illegal_opcode`, `fault_execution_error`) through debug snapshots and pause events.
- 2026-03-09: `A8E/README.md`: clarified the native Windows/MSVC `vcpkg` instructions so `vcpkg` is treated as an external toolchain checkout, not something embedded inside this repository. The example path now uses `C:\dev\external\vcpkg` and states explicitly that any external path is fine.
- 2026-03-09: removed the temporary repo-local `tools/jsa8e_runner.mjs` harness and bundled preset files so the current tree stays focused on the browser-first `window.A8EAutomation` surface while the next automation design is planned. Root/browser README automation notes and the jsA8E automation architecture note were updated to remove the old runner-specific guidance.
- 2026-03-09: `implementation/jsA8E/AUTOMATION.md`: extended the public machine API plan with explicit stop/fault semantics. The plan now defines stable pause reasons such as `fault_illegal_opcode` and `fault_execution_error`, plus post-fault inspection expectations so agents can detect illegal instructions and inspect registers, trace, source context, and disassembly through the browser API.
- 2026-03-09: `implementation/jsA8E/AUTOMATION.md`: expanded the automation architecture plan with explicit paused-state inspection goals. The plan now includes `debug.getSourceContext(...)` for mapped source neighborhoods and `debug.disassemble(...)` for live memory disassembly around the current PC, so agent workflows can assemble, run, pause, and inspect both CPU state and nearby code context through the public API.
- 2026-03-09: `implementation/jsA8E/AUTOMATION.md`: expanded the browser-first automation plan with explicit assembler-as-API scenarios (`assembleSource`, `runXex`) and a concrete timed-XEX screenshot workflow. The plan now calls out source-text assembly as a first-class public capability for agent-driven assembler/testbed use.
- 2026-03-09: added `implementation/jsA8E/AUTOMATION.md` as the architectural plan for a universal browser-first jsA8E machine API. It defines the abstraction goals, ownership boundaries, domain-grouped public API shape, consistency rules, and phased roadmap so future automation work improves emulator clarity instead of creating parallel control paths.
- 2026-03-09: `jsA8E/js/app/automation_api.js`, `jsA8E/js/app/ui.js`, `jsA8E/js/core/{app_proxy,atari,cpu,debugger,memory}.js`, `jsA8E/emulator_worker.js`: added a stable browser automation surface (`window.A8EAutomation`) plus worker RPC for debugger/memory/artifact requests. Runtime now exposes deterministic paused-mode execution controls (`stepInstruction`, `stepOver`, `runUntilPc`, pause-on-address/bounded runs), debug counters (`cycleCounter`, `instructionCounter`), bank/memory introspection, a rolling instruction trace tail, framebuffer-based PNG screenshot capture, and JSON artifact collection.
- 2026-03-09: `README.md`, `A8E/README.md`, `jsA8E/README.md`: updated the top-level and subproject READMEs to describe the current raster-accuracy state after the recent color-clock timing pass. The docs now state that visible scanlines use per-color-clock rendering and PMG interleaving in both cores, while still calling out the remaining geometry / non-wide `HSCROL` timing gaps and pointing readers to `legacy/COLOR_CLOCK_ACCURACY.md` for the detailed checklist.
- 2026-03-09: `jsA8E/js/core/playfield.js`: completed the JS PMG interleave rollout on visible blank/background-only scanlines. The blank-line background path now uses the same HSYNC-based X basis as the per-clock PMG span hook, so interleaved PMG pixels on those lines are no longer overpainted a few clocks later. A headless sweep across the repo's display-list disks stayed bit-identical to the previous active-line-only JS PMG state while removing the last visible-scanline post-line overlay fallback.
- 2026-03-09: `jsA8E/js/core/atari.js`, `jsA8E/js/core/antic.js`, `jsA8E/js/core/playfield.js`: re-enabled JS player/missile interleaving on active playfield scanlines while keeping blank/background-only scanlines on the older post-line overlay path. This recovers the per-color-clock PMG path on the timed playfield draw loop without reintroducing the earlier black DLI-line regression in the sampled display-list boots; remaining JS PMG gaps are now limited to blank/background-only lines and a small localized difference in a couple of display-list titles.
- 2026-03-09: `A8E/AtariIo.c`, `jsA8E/js/core/playfield.js`: visible blank/background-only scanlines in both cores now spend the first 6 color-burst clocks as invisible timing before painting the remaining 108 clocks of live-read background. This removes the previous full-114-clock blank-line paint behavior and narrows the remaining raster-accuracy gap on blank display-list lines. The JS PMG path remains on the restored post-line overlay path.
- 2026-03-09: `jsA8E/js/core/antic.js`, `jsA8E/js/core/atari.js`, `jsA8E/js/core/playfield.js`: restored the JS core to the previous post-line PMG overlay path after the per-clock PMG experiment regressed some DLI-driven raster content and produced black lines. Native C keeps the new per-color-clock PMG path; JS is temporarily back on the last known-good whole-line overlay until the DLI regression is understood.
- 2026-03-09: `A8E/AtariIo.c`, `jsA8E/js/core/gtia.js`, `jsA8E/js/core/antic.js`, `jsA8E/js/core/atari.js`: player/missile rendering now runs on the active per-color-clock draw path in native C instead of as a post-line overlay. The C implementation now rereads live PMG DMA/register state on each clock, clips PMG compositing to the current 4-pixel span, and accumulates collisions incrementally while drawing. The matching JS interleave path is still in `gtia.js` for reference but is not currently wired into the JS scanline loop because of the DLI regression above. Remaining color-clock accuracy work is blank-line color-burst behavior, JS PMG interleave recovery, and raster regression verification against the existing checklist.
- 2026-03-09: `A8E/AtariIo.c`, `jsA8E/js/core/playfield.js`: active ANTIC playfield lines in both cores now derive their timing from an explicit legacy-style `color burst -> left border -> playfield -> right border` geometry helper instead of the old `renderStartX` / `preCycles` approximation. Both implementations now derive `bytesPerLine` from the selected playfield cycle budget, spend the color-burst clocks separately before drawing the live-read left border, and place playfield output using the fixed HSYNC pixel offset plus the legacy scroll offset. Remaining accuracy gaps are still PMG interleaving, blank-line color-burst behavior, and regression verification against the raster checklist.
- 2026-03-08: `A8E/AtariIo.c`, `jsA8E/js/core/playfield.js`, `legacy/COLOR_CLOCK_ACCURACY.md`: active playfield scanlines now emit their left/right background segments through the same per-color-clock path used by blank lines instead of repainting borders from a post-render snapshot. Both cores now advance invisible pre-playfield clocks separately, reread live background state while drawing border clocks, and leave the clipped offscreen wide-playfield region on the original playfield path. Remaining work is raster verification against the existing regression checklist.
- 2026-03-08: `A8E/AtariIo.c`, `jsA8E/js/core/playfield.js`, `legacy/COLOR_CLOCK_ACCURACY.md`: blank / no-playfield scanlines in both cores now render through the per-color-clock path instead of filling the whole line from a single background snapshot. Visible blank-line output now rereads live `PRIOR`/background state on each clock while advancing timed events, and both implementations now write background priority data consistently on those lines. Remaining raster-accuracy work is still behavior verification, especially around clipped border output during active playfield lines.
- 2026-03-08: `A8E/AtariIo.c`, `jsA8E/js/core/playfield.js`, `legacy/COLOR_CLOCK_ACCURACY.md`: completed the per-color-clock playfield pass for ANTIC Modes 8-E in both cores. These modes now perform inline display-byte DMA steals on the first scanline of each row, reread live playfield colors while emitting pixels, and run `AtariIo_DrawClockAction()` / `clockAction()` on every playfield color clock. The native Mode A renderer was also corrected to follow the legacy 2-bit color decode path while making this timing pass. Remaining work is behavior verification against the existing raster regression checklist.
- 2026-03-08: `A8E/AtariIo.c`, `jsA8E/js/core/playfield.js`, `legacy/COLOR_CLOCK_ACCURACY.md`: extended the per-color-clock playfield pass from Mode F into ANTIC Modes 3-7 in both cores. These modes now run `AtariIo_DrawClockAction()` / `clockAction()` inside their playfield loops, apply inline character/pattern DMA steals at fetch points, and reread live color / PRIOR state where the mode logic depends on it. The remaining open raster-accuracy work is now concentrated in Modes 8-E plus behavior verification.
- 2026-03-08: `A8E/AtariIo.c`, `jsA8E/js/core/playfield.js`, `legacy/COLOR_CLOCK_ACCURACY.md`: extended the per-color-clock playfield pass to ANTIC Mode F in both cores. Mode F now performs one inline DMA steal at each display-byte fetch, rereads `PRIOR` and dependent color registers on each playfield cycle, and runs `AtariIo_DrawClockAction()` / `clockAction()` inside the mode loop. The timing checklist now reflects that scanline ownership and next-line suppression are implemented, while the remaining per-mode live-read/DMA work stays explicitly open.
- 2026-03-07: `A8E/AtariIo.c`, `A8E/AtariIo.h`, `jsA8E/js/core/antic.js`, `jsA8E/js/core/playfield.js`, `jsA8E/js/core/state.js`, `legacy/COLOR_CLOCK_ACCURACY.md`: aligned the active-scanline timing model with the `legacy/A8E_experimental` porting rules. Both cores now exclude only next-scanline display-list fetch from timed-event dispatch while a line draw is active, instead of globally suppressing events; DLI/timer/serial events remain live during rendering. Mode 2 PRIOR/inverse handling in both cores now derives per-cycle output data from live PRIOR instead of mutating fetched byte data.
- 2026-03-06: `jsA8E/js/core/playfield.js`: fixed the first scratch-buffer regression from the S3/S4 raster pass. The playfield line copy-back step now writes from the scratch buffers into the real screen pixel/priority buffers; the initial version was still targeting `video.pixels`/`video.priority` while those pointers were temporarily redirected to scratch storage, so some playfield graphics never became visible on screen.
- 2026-03-06: `jsA8E/js/core/cpu.js`, `jsA8E/js/core/playfield.js`, `jsA8E/js/render/software.js`: improved JS raster timing and HSCROL rendering for the S3/S4 accuracy pass. The CPU run loop now stops on pending timed-event boundaries before starting an instruction that would overrun them, allowing DLI/WSYNC-driven register writes to take effect before the next ANTIC draw event. Playfield rendering now uses a wider per-frame scratch buffer for each scanline so wide-playfield + HSCROL lines use consistent extra-byte fetch geometry instead of the old `pfWidth=3` offset hack, then clips the visible 456-pixel window back into the display buffer.
- 2026-03-06: Deferred hardware-accuracy follow-up (from S3/S4 screenshot report): after applying DLI ordering fixes in C/JS, still verify remaining symptoms in both cores: (1) HSCROL-centered text and wide-playfield positioning, (2) missing/corrupted charset/playfield layers during visible-frame DLI activity, (3) per-scanline player/background color cadence driven by one DLI + scanline counter logic, and (4) audio distortion when music playback runs during visible-frame drawing. Keep this as an explicit regression checklist for the next emulation-accuracy pass.
- 2026-03-06: DLI timing regression correction in both cores (`A8E/AtariIo.c`, `jsA8E/js/core/antic.js`): restored DLI scheduling to trigger on the last scanline of the current display-list instruction (`nextDisplayListLine - currentDisplayLine - 1`) and restored timed-event order to process display-list fetch before DLI dispatch in the same cycle tick. This reverts the one-scanline-late DLI behavior introduced with the `4a61ca4` line and preserves immediate DLI dispatch for single-scanline blank commands.
- 2026-03-05: `A8E/CMakeLists.txt`: removed CMake-integrated formatting setup (`A8E_LLVM_BIN_HINTS`, `A8E_ENABLE_FORMAT_TARGET`, and `a8e-format` target) to keep native build config minimal.
- 2026-03-05: `A8E/CMakeLists.txt`: removed `clang-tidy` enforcement wiring from CMake (including `A8E_ENFORCE_BRACES` option and `CMAKE_C_CLANG_TIDY` setup) to keep build configuration simpler.
- 2026-03-05: `A8E/CMakeLists.txt`, `A8E/.clang-format`, `A8E/.clang-tidy`: native C tooling now auto-discovers Homebrew LLVM binaries on macOS (`/opt/homebrew/opt/llvm/bin`, `/usr/local/opt/llvm/bin`), enables `clang-tidy` via project config (`readability-braces-around-statements` as error), and adds an `a8e-format` CMake target that runs `clang-format` across A8E C/H sources. Tooling was applied in-tree (`cmake --build build --target a8e-format`) and native rebuild passes.
- 2026-03-05: `A8E/6502.c`, `A8E/A8E.c`, `A8E/Antic.c`, `A8E/AtariIo.c`, `A8E/Gtia.c`, `A8E/Pia.c`, `A8E/Pokey.c`: tidied native C control-flow style by adding explicit braces to formerly single-statement `if/for/while` bodies (line-range `clang-format` with `InsertBraces`), aligned with new brace-enforcement direction while preserving behavior (native target rebuilds successfully).
- 2026-03-05: `A8E/CMakeLists.txt`: added configurable C brace enforcement in native builds via `clang-tidy` (`readability-braces-around-statements`) with warnings promoted to errors. New CMake option `A8E_ENFORCE_BRACES` defaults to `ON`; configuration now fails with a clear message when enabled but `clang-tidy` is unavailable.
- 2026-03-05: `jsA8E/js/core/assembler/preprocessor.js`, `jsA8E/js/core/assembler/assembler.js`, `jsA8E/js/core/assembler/object_writer.js`, `jsA8E/js/core/assembler_core.js`: preprocessor now accepts injected initial defines from assembler options (`defines`/`preprocessorDefines`/`initialDefines`) and `.const(...)` accepts expression/macro-argument forms (not only bare identifiers). Assembler emission now accepts external import value injection (`importValues`/`externals`/`imports`) when evaluating instruction/data expressions, supports optional deferred `.assert` handling via `deferAsserts`, and exposes relocatable object emission (`assembleToObject`) using new `A8OBJ` payload builder/parser (`buildA8Object`/`parseA8Object`).
- 2026-03-05: `jsA8E/js/core/assembler/lexer.js`, `jsA8E/js/core/assembler/parser.js`, `jsA8E/js/core/assembler/preprocessor.js`: expanded assembler expression support to precedence-based operators (`* / << >> & | ^ ~ ! == != <= >= && ||` plus existing low/high-byte unary `<`/`>`), added cheap local labels (`@name`/`?name`) scoped to the most recent non-local label, added forced-absolute operand overrides for direct/indexed addressing (`!expr`, `A:expr`, `ABS:expr`), and aligned preprocessor expression/`.include` parsing with escaped-string + `==`/`!=` handling.
- 2026-03-05: `jsA8E/js/core/assembler/shared.js`, `jsA8E/js/core/assembler/parser.js`, `jsA8E/js/core/assembler/assembler.js`: added `END/.END` directive handling to the modular assembler core. `END` is now treated as a real directive (not as a label), optionally accepts a run expression (`END expr`), and terminates further source parsing for the current file.
- 2026-03-05: `jsA8E/js/core/assembler/assembler.js`: byte/word emission now accepts signed constants in 8/16-bit assembler contexts (byte: `-128..255`, word: `-32768..65535`) and encodes them as two's complement (`& $FF`/`& $FFFF`). This fixes immediate forms like `CMP #-2` while still rejecting true overflow (for example `#-129`).
- 2026-03-05: `jsA8E/js/core/assembler/parser.js`, `jsA8E/js/core/assembler/assembler.js`, `jsA8E/js/core/assembler/shared.js`, `jsA8E/js/core/assembler/preprocessor.js`: added parser/emit support for additional ca65/GEOS-style directives in the browser assembler core: `.segment`, `.import`, `.global`, `.res`, `.addr`, `.lobytes`, `.hibytes`, plus assembler-stage `.assert` and `.error`. Current XEX mode semantics: `.segment`, `.import`, and `.global` are accepted as metadata/no-ops for now; `.res` supports optional fill (`.res count[,fill]`), `.addr` aliases `.word`, and `.lobytes`/`.hibytes` emit low/high bytes per expression.
- 2026-03-05: `jsA8E/js/core/assembler/preprocessor.js`, `jsA8E/js/core/assembler/assembler.js`, `jsA8E/js/core/assembler/shared.js`, `jsA8E/js/core/assembler_core.js`, `jsA8E/js/app/assembler_ui.js`, `jsA8E/index.html`: added a real assembler preprocessor stage in the modular core with support for `.include`, `.define`/`.undef`, `.macro`/`.endmacro` (`.endm` alias), `.local` macro locals, and conditional assembly (`.if`, `.ifdef`, `.ifndef`, `.elseif`, `.else`, `.endif`) including expression operators and builtins (`.defined`/`.def`/`.const`, `.not`, `.or`). UI assembly now passes `sourceName` + HostFS-backed include resolver options to core so includes resolve during build.
- 2026-03-05: `jsA8E/js/core/assembler/shared.js`, `jsA8E/js/core/assembler/lexer.js`, `jsA8E/js/core/assembler/parser.js`, `jsA8E/js/core/assembler/object_writer.js`, `jsA8E/js/core/assembler/assembler.js`, `jsA8E/js/core/assembler_core.js`, `jsA8E/index.html`: phase-2 assembler-core modularization. The core assembler pipeline is now split into internal modules (shared tables/utils, lexer/expression parsing, parser/layout pass, object writer, and assemble/orchestration), and `assembler_core.js` is now a thin facade that wires modules and exposes `window.A8EAssemblerCore`.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`: completed phase-1 UI/core split cleanup by removing the duplicated in-UI assembler engine (parsing/layout/emit helpers and local `assembleToXex`) and keeping a thin `assembleSourceToXex` proxy that calls `window.A8EAssemblerCore`. UI now focuses on editor/highlight/debugger panel behavior.
- 2026-03-05: `jsA8E/js/core/assembler_core.js`, `jsA8E/index.html`, `jsA8E/js/app/assembler_ui.js`: phase-1 assembler architecture split started by extracting the assembler engine into a reusable `A8EAssemblerCore` module (loaded after `cpu_tables.js`) and wiring the UI to delegate assembly through the core. UI keyword highlighting/reserved-token detection now consumes core-exported mnemonic/directive keyword lists.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`: assembler expression parser now treats square brackets as grouping (like parentheses) in both argument splitting and expression term parsing, enabling MADS-style forms such as `LDA # <[MAZEDAT-$84]`.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`: improved MADS-style compatibility for assembler directives by accepting bare and dotted forms for existing directives (for example `ORG $80` and `.ORG $80`), and adding `DS/.DS` storage reservation support. `.DS` now advances the location counter without emitting bytes (segment gap), enabling label allocation patterns like `PL2 .DS $0100` / `PL3 .DS $0100`.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`, `jsA8E/style.css`: assembler breakpoint gutter now gets a `has-build` visual state after successful build output is available (address and/or bytes maps). In this state, assembled-line breakpoint markers and line-number column are rendered with higher contrast so the breakpoint column is clearly visible after build.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`, `jsA8E/style.css`: assembler build results now include per-source-line emitted byte lists, shown in a new bytes column in the breakpoint gutter (with full byte text in row tooltips). Debug controls are also activated immediately after a successful assemble (Build or Run path), so stepping/continue buttons are available as soon as code has been assembled.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`: assembler debug control state now has explicit `hidden/armed/paused/running` modes. While emulation is running, the former `Continue` action is shown as `Pause` (same button) and calls `app.pause()`, then returns to paused stepping controls (`Step`, `Step Over`, `Continue`). This provides an in-panel pause path for live breakpoint editing during execution.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`: when execution pauses with `reason=breakpoint`, assembler debug navigation now jumps caret/scroll directly to the mapped source line (if address-to-line mapping exists), so breakpoint hits immediately reveal the active source location.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`: assembler debugger line tracking now auto-scrolls the editor when paused `step` lands on a different mapped source line (for example after stepping into `JSR`/`JMP` targets). Auto-scroll is conditional on available address-to-line mapping and does not alter caret/selection.
- 2026-03-05: `jsA8E/js/core/debugger.js`, `jsA8E/js/core/atari.js`, `jsA8E/index.html`, `jsA8E/emulator_worker.js`, `implementation/jsA8E/DEBUG.md`: debug runtime responsibilities (breakpoint hooks, debug state broadcasting, step/step-over flow, and breakpoint resume/hit bookkeeping) were extracted from `atari.js` into a dedicated `core/debugger.js` module and wired into both main-thread and worker boot script lists. `atari.js` now delegates debugger operations via the new runtime API while preserving existing public app methods and behavior.
- 2026-03-05: `jsA8E/index.html`, `jsA8E/style.css`, `jsA8E/js/app/assembler_ui.js`, `jsA8E/js/app/ui.js`, `jsA8E/js/core/atari.js`, `jsA8E/js/core/cpu.js`, `jsA8E/js/core/app_proxy.js`, `jsA8E/emulator_worker.js`: assembler editor now includes a dedicated breakpoint gutter with per-line toggles; successful assembly exports line-to-address mappings, and active line breakpoints are pushed dynamically to runtime PC hooks. Runtime now supports pausing on breakpoint hits plus debug stepping (`Step`, `Step Over`, `Continue`) in both main-thread and worker mode. While paused in debugger mode, the assembler `Run` action is hidden and restored after `Continue`. Assembler status bar appends live debug registers (`PC/A/X/Y/SP/P`) plus breakpoint-hit context.
- 2026-03-05: `jsA8E/js/app/assembler_ui.js`: assembler label parsing now accepts leading labels without a trailing colon (both label-only lines and `label <statement>` forms), while preserving constant-definition parsing (`NAME = expr`, `NAME EQU expr`) and existing mnemonic/directive handling.
- 2026-03-03: `jsA8E/js/app/assembler_ui.js`, `jsA8E/style.css`: fixed assembler panel stability for long sources by removing conflicting editor minimum sizing (`.asm-editor-wrap` can now flex/shrink), raising panel minimum to `340px`, resetting editor scroll/caret to top on source load, and using focus-without-scroll when opening the panel so the assembler toolbar/menu stays visible.
- 2026-03-03: `jsA8E/js/app/assembler_ui.js`, `jsA8E/style.css`: assembler panel now opens taller by default (screen height + extra headroom, clamped to viewport), with a larger minimum panel/editor height (`280px` panel minimum, `300px` editor area minimum) for a bigger editing workspace.
- 2026-03-03: `jsA8E/js/app/assembler_ui.js`: assembler panel initial height now matches the emulator display screen (`.screenViewport`) height, so the editor and screen are visually aligned. Still resizable via the bottom drag handle (min 220px).
- 2026-03-03: `jsA8E/js/app/assembler_ui.js`: assembler panel now auto-loads the first `.ASM` source file from HostFS when opened with an empty editor; if no source files exist on HostFS the editor stays clear.
- 2026-03-03: `jsA8E/index.html`, `jsA8E/style.css`: HostFS panel toolbar restructured for consistency with assembler panel. Upload/Folder buttons moved from titlebar into toolbar with unified `hostfs-btn` base class. Toolbar now uses two labeled groups (Add: Upload, Folder | Select: All, None, Download, Delete). Titlebar is now purely informational (icon + title), matching assembler panel conventions. Removed separate titlebar button CSS rules and unused `.hostfs-title-path`.
- 2026-03-03: `jsA8E/index.html`, `jsA8E/style.css`, `jsA8E/js/app/assembler_ui.js`: assembler toolbar streamlined to two logical groups (File | Build). Removed the separate output filename input; `.XEX` name is now auto-derived from the source name. Run now auto-saves source before assembling. Added keyboard shortcuts: `Ctrl+S` save, `Ctrl+Shift+B` assemble, `Ctrl+Enter` run.
- 2026-03-03: `jsA8E/style.css` UI consistency pass: extracted shared sub-panel CSS custom properties (`--sub-panel-*`) for border, background, button radius, toolbar, and statusbar colors; normalized button padding/radius (`4px` radius, `3px 8px` padding), toolbar gap (`5px`) and padding (`5px 8px`), statusbar padding (`4px 10px`), disabled opacity (`0.35`), and separator margin across HostFS and Assembler panels; unified top-bar button border-radius to `6px`; added matching WebKit scrollbar styling to the assembler editor and error list.
- 2026-03-03: `jsA8E/version.json` runtime build version updated from `v1.2.0` to `v1.3.0` to prepare the next release.
- 2026-03-03: `jsA8E/index.html`, `jsA8E/style.css`, `jsA8E/js/app/assembler_ui.js`, and `jsA8E/js/app/ui.js` now add an assembler `Run` action next to `Assemble`: it assembles, writes the `.XEX` to HostFS, mounts it to `D1:` through the same `loadDiskToDeviceSlot` path used by disk loading, and then resets/starts the emulator for a one-click launch. The assembler panel height was also increased (`min-height` and editor area floor) for a larger editing workspace.
- 2026-03-03: `jsA8E/index.html`, `jsA8E/style.css`, and `jsA8E/js/app/assembler_ui.js` now provide built-in syntax highlighting in the assembler editor without external dependencies: a mirrored `<pre>` highlight layer is synchronized with textarea input/scroll, with token coloring for comments, labels, directives, mnemonics, numbers, strings, and register names.
- 2026-03-03: `jsA8E/js/app/assembler_ui.js` assembler layout now resolves instruction sizes iteratively (up to convergence) using prior-pass symbol hints, so forward references can correctly select zero-page encodings (`ZP/ZPX/ZPY`) instead of being locked to absolute mode. The assembler now also returns structured error lists (`errors[]`) and can recover enough to report multiple line errors in one build attempt. `jsA8E/index.html` + `jsA8E/style.css` add an assembler error list panel with clickable line navigation in the editor.
- 2026-03-03: `jsA8E/js/core/hdevice.js` H: `GET BYTES` CIO behavior now reports `SUCCESS` on short final reads when bytes were transferred, and `EOF` only when zero bytes are returned. This aligns better with DOS binary loaders that treat immediate EOF on partial reads as an error path.
- 2026-03-03: `jsA8E/js/app/assembler_ui.js` assembler now supports constant definitions (`NAME=expr`, `NAME EQU expr`, `.EQU/.SET name,expr`), enabling source like `DOSVEC=$000A` to assemble successfully and be referenced in instructions/directives.
- 2026-03-03: `jsA8E/index.html` adds a new assembler editor toolbar icon (`btnAssembler`) and `assemblerPanel`; `jsA8E/style.css` adds matching panel/theme styles; `jsA8E/js/app/ui.js` now initializes `window.A8EAssemblerUI`; `jsA8E/js/app/assembler_ui.js` adds HostFS-integrated source load/save plus a built-in two-pass 6502 assembler that writes DOS-loadable `.XEX` executables to H: (including RUNAD segment emission).
- 2026-02-23: `.github/workflows/build.yml` now packages Linux/macOS release artifacts as standalone `tar.gz` bundles that include SDL runtime libraries (`libSDL-1.2.so.0` or `libSDL-1.2.0.dylib`) and adds packaging-time dependency checks (`ldd`/`otool`) to fail on unresolved or Homebrew-linked SDL paths.
- 2026-02-23: `jsA8E/version.json` runtime build version updated from `v1.1.1` to `v1.2.0`.
- 2026-02-23: HostFS directory behavior is now explicitly documented as a design choice: keep a flat filename namespace (no subdirectory hierarchy) for Atari DOS/FMS-style compatibility in the H: workflow.
- 2026-02-23: `jsA8E/js/app/hostfs_ui.js` status bar now shows DOS-style `FREE SECTORS` in the HostFS panel using the same logical model as H: (`999` sectors, `128` bytes per sector, per-file sector rounding), alongside total byte size.
- 2026-02-23: `jsA8E/js/core/hdevice.js` now computes H: `FREE SECTORS` from file-size math (128-byte sectors) with a 999-sector logical capacity budget instead of subtracting file count, and returns `disk full` on `CLOSE` when a write would exceed that budget.
- 2026-02-23: `implementation/jsA8E/UI.md` documents HostFS flat-namespace behavior for H: workflows: subfolder structure is flattened to basename-only entries, so duplicate filenames across subfolders can collide/overwrite.
- 2026-02-23: `jsA8E/index.html` renames the H: file-manager visible labels from `H: Files`/`H: files` to `HostFS`; the panel headline now explicitly shows the drive marker as `HostFS (H:)`.
- 2026-02-23: `jsA8E/style.css` now sizes `hostfsPanel` to keyboard width (`min(100%, 1083px)`), centers it in the layout, and uses a reduced panel/list height baseline (`clamp(220px, 36vh, 420px)` panel, 96 px list floor). `jsA8E/js/app/hostfs_ui.js` now uses lighter dynamic list minimums (150 px empty, 96 px non-empty) with the original 60 px bottom margin and 60% viewport cap.
- 2026-02-23: `.github/workflows/build.yml` now includes a dedicated `build-macos-arm64` job on `macos-14` that builds `arm64` binaries and publishes `A8E-<tag>-macos-arm64` release artifacts.
- 2026-02-23: `A8E/AtariIo.c` ANTIC modes 2 and 3 now snapshot `IO_PRIOR` high bits once per scanline draw call and reuse that value for inversion and mode branching. This aligns C behavior with existing JS/C mode F snapshot semantics.
